plugins {
  id 'org.hidetake.ssh' version '1.1.4'
}

remotes {
  ubuntu01 {
    host     = '***'
    user     = '***'
    password = '***'
    knownHosts = allowAnyHosts 
  }
}


task testSsh << {
  ssh.run {
    session(remotes.ubuntu01) {
      execute('uname -a') { result ->
        println result
      }
    }
  }
}
